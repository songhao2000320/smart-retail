<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.retail.mapper.ZoneStayMapper">

    <select id="selectByBehaviorId" resultType="com.retail.entity.ZoneStay">
        SELECT
            zs.*,
            z.name AS zone_name
        FROM t_zone_stay zs
        LEFT JOIN t_zone z ON zs.zone_id = z.id
        WHERE zs.behavior_id = #{behaviorId}
        ORDER BY zs.entry_time
    </select>

</mapper>
